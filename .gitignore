## Yarn
.yarn/*
!.yarn/patches 
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

### Should likely always be ignored since they typically hold machine-specific build artifacts.
### Ignoring it might however prevent Zero-Installs from working (to prevent this, set `enableScripts` to false).
### Documentation here 👉 https://yarnpkg.com/getting-started/qa#which-files-should-be-gitignored
.yarn/unplugged

### Commment when not using zero-installs
### In that case, don't forget to run `yarn config set enableGlobalCache false`!
### Documentation here 👉 https://yarnpkg.com/features/caching#zero-installs
# !.yarn/cache

### Comment when using zero-installs
### In that case, don't forget to remove `enableGlobalCache` option or set it to false!
.pnp.*

## Yarn Integrity file
.yarn-integrity

## build output
dist/

## generated types
.astro/

## dependencies
node_modules/

## logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


## environment variables
.env
.env.*

## macOS-specific files
.DS_Store
